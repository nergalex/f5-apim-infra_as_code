{
  "metadata": {
    "name": "api_{{ extra_app.name | lower }}_{{ item.name | lower}}",
    "displayName": "Published API of component {{ item.name | lower }}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "basePath": "{{ item.uri }}",
    "apiDefinitionVersionRef": {
      "ref": "/services/api-definitions/api_{{ extra_app.name | lower }}_{{ item.name | lower}}/versions/{{ item.version | lower}}"
    },
    "gatewayRefs": [
      {
        "ref": "/services/environments/{{ var_env_prefix }}{{ extra_app.environment | lower }}/gateways/gw_{{extra_app.name | lower}}"
      }
    ]
  }
}