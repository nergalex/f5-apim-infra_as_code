{
  "metadata": {
    "name": "crt_{{extra_app.name | lower}}",
    "displayName": "Certificate {{extra_app.name | lower}}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "type": "PEM",
    "privateKey": "{%- for line in extra_app_tls_key.split('\n') -%}{{ line }}\n{%- endfor -%}",
    "publicCert": "{%- for line in extra_app_tls_crt.split('\n') -%}{{ line }}\n{%- endfor -%}"
  }
}